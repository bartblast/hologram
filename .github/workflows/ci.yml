name: CI

# Define workflow that runs when changes are pushed to the
# `dev` or `master` branch or pushed to a PR branch that targets the `dev`
# or `master` branch.
on:
  push:
    branches:
      - "**"
  pull_request:
    branches:
      - "**"

# Sets the ENV `MIX_ENV` to `test` for running tests
env:
  MIX_ENV: test

permissions:
  contents: read

jobs:
  lib_tests:
    runs-on: ${{ matrix.os }}
    name: Lib Tests on ${{ matrix.os }}, Elixir ${{ matrix.elixir }}, Erlang/OTP ${{ matrix.otp }}, Node.js ${{ matrix.node }}
    strategy:
      fail-fast: false
      # Each supported Elixir version is tested against every Erlang/OTP major it supports
      # within the supported OTP range, since that is where OTP and type-system
      # differences are most likely to surface.
      # OTP compatibility per Elixir version:
      # https://hexdocs.pm/elixir/compatibility-and-deprecations.html#between-elixir-and-erlang-otp
      #
      # Notes on the bounds:
      # - Client-side regex support requires PCRE2 semantics (OTP 28+) and :re.import/1 (OTP 28.1+),
      #   so OTP 28.1 is the floor.
      # - Elixir 1.19 is the oldest version cleanly pairing with OTP 28, so 1.19 is the floor.
      # - Underlying older constraints, superseded by the floors above: multi-letter uppercase
      #   sigils (~HOLO) were introduced in Elixir 1.15.0, Elixir 1.15+ requires Erlang/OTP 24+,
      #   and cowlib 2.14+ requires OTP 23+.
      # - Node.js only builds the JS step (actual JS runs in browsers), so we pin the latest LTS
      #   only, matching .tool-versions for dev/prod/CI parity. Floor rationale: the Web Crypto API
      #   requires Node 19+, and a few JS libs used by Hologram require Node 20+.
      # - Windows only smoke-tests the newest Elixir across its OTP range: Windows failures are
      #   OS-specific (paths, shell), not Elixir-version-specific, so Ubuntu carries full coverage.
      #   TODO: change windows-2022 back to windows-latest when erlef/setup-beam supports win25.
      matrix:
        include:
          - os: ubuntu-latest
            elixir: "1.19.5"
            otp: "28.5"
            node: "24.13.0"
          - os: ubuntu-latest
            elixir: "1.20.0"
            otp: "28.5"
            node: "24.13.0"
          - os: ubuntu-latest
            elixir: "1.20.0"
            otp: "29.0"
            node: "24.13.0"
          - os: windows-2022
            elixir: "1.20.0"
            otp: "28.5"
            node: "24.13.0"
          - os: windows-2022
            elixir: "1.20.0"
            otp: "29.0"
            node: "24.13.0"
    env:
      IS_HIGHEST_MATRIX_COMBINATION: ${{ contains(matrix.os, 'ubuntu-latest') && contains(matrix.elixir, '1.20.0') && contains(matrix.otp, '29.0') && contains(matrix.node, '24.13.0') }}
    steps:
      # Step: Setup Elixir + Erlang/OTP image as the base.
      - name: Set up Elixir
        uses: erlef/setup-beam@v1
        with:
          hexpm-mirrors: |
            https://builds.hex.pm
            https://cdn.jsdelivr.net/hex
          elixir-version: ${{ matrix.elixir }}
          otp-version: ${{ matrix.otp }}

      # Step: Checkout the code.
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      # Step: Setup Node.js.
      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: ${{ matrix.node }}

      ### LIB CHECKS ###

      # Step: Define how to cache the lib Elixir dependencies.
      - name: Cache lib Elixir deps
        id: cache-lib-elixir-deps
        uses: actions/cache@v6
        env:
          cache-name: cache-lib-elixir-deps
        with:
          path: deps
          key: ${{ env.cache-name }}-${{ runner.os }}-${{ matrix.elixir }}-${{ matrix.otp }}-${{ hashFiles('mix.lock') }}

      # Step: Define how to cache the lib Elixir build.
      - name: Cache lib Elixir build
        id: cache-lib-elixir-build
        uses: actions/cache@v6
        env:
          cache-name: cache-lib-elixir-build
        with:
          path: _build
          key: ${{ env.cache-name }}-${{ runner.os }}-${{ matrix.elixir }}-${{ matrix.otp }}-${{ hashFiles('mix.lock') }}

      # Step: Download lib Elixir dependencies.
      # If unchanged, uses the cached version.
      - name: Download lib Elixir deps
        uses: nick-fields/retry@v4
        with:
          command: mix deps.get
          timeout_minutes: 3
          max_attempts: 10

      # Step: Download lib JavaScript dependencies.
      - name: Download lib JavaScript deps
        uses: nick-fields/retry@v4
        with:
          command: cd assets && npm clean-install
          shell: bash
          timeout_minutes: 3
          max_attempts: 10

      # Step: Compile the lib project treating any warnings as errors.
      - name: Check lib project compiles without warnings
        run: mix compile --all-warnings --warnings-as-errors

      # Step: Check if lib mix.lock has any unused dependencies.
      - name: Check lib unused Elixir deps
        if: ${{ env.IS_HIGHEST_MATRIX_COMBINATION == 'true' }}
        run: mix deps.unlock --check-unused

      # Step: Check if there are any lib Elixir dependencies which have been marked as retired.
      - name: Check lib retired Elixir deps
        if: ${{ env.IS_HIGHEST_MATRIX_COMBINATION == 'true' }}
        run: mix hex.audit

      # Step: Check if there are any security vulnerabilities in the lib Elixir dependencies.
      - name: Check lib security vulnerabilities in Elixir deps
        if: ${{ env.IS_HIGHEST_MATRIX_COMBINATION == 'true' }}
        run: mix deps.audit

      # Step: Check that the lib Elixir code has already been formatted.
      - name: Check lib Elixir code formatted
        if: ${{ env.IS_HIGHEST_MATRIX_COMBINATION == 'true' }}
        run: mix format --check-formatted

      # Step: Check that the lib JavaScript, JSON & YAML code has already been formatted.
      - name: Check lib JavaScript, JSON & YAML code formatted
        if: ${{ env.IS_HIGHEST_MATRIX_COMBINATION == 'true' }}
        run: mix format.js.check

      # Step: Check that lib doc and spec coverage are above thresholds (with Doctor).
      - name: Check lib doc and spec coverage
        if: ${{ env.IS_HIGHEST_MATRIX_COMBINATION == 'true' }}
        run: mix doctor

      # Step: Run lib Elixir static code analysis with Credo.
      - name: Run lib Elixir static code analysis with Credo
        if: ${{ env.IS_HIGHEST_MATRIX_COMBINATION == 'true' }}
        run: mix credo --strict

      # Step: Run lib Elixir security-focused analysis with Sobelow.
      - name: Run lib Elixir security-focused analysis with Sobelow
        if: ${{ env.IS_HIGHEST_MATRIX_COMBINATION == 'true' }}
        run: mix sobelow --config

      # Step: Restore lib Dialyzer PLT cache.
      # Cache key is based on Erlang/Elixir version and the mix.lock hash.
      - name: Restore lib Dialyzer PLT cache
        id: restore-lib-dialyzer-plt-cache
        uses: actions/cache/restore@v6
        with:
          key: |
            dialyzer-plt-lib-${{ runner.os }}-${{ matrix.elixir }}-${{ matrix.otp }}-${{ hashFiles('mix.lock') }}
          path: |
            priv/plts
        if: ${{ env.IS_HIGHEST_MATRIX_COMBINATION == 'true' }}

      # Step: Create lib Dialyzer PLTs if no cache was found.
      - name: Create lib Dialyzer PLTs
        if: ${{ env.IS_HIGHEST_MATRIX_COMBINATION == 'true' && steps.restore-lib-dialyzer-plt-cache.outputs.cache-hit != 'true' }}
        run: mix dialyzer --plt

      # Step: Save lib Dialyzer PLT cache.
      # By default, the GitHub Cache action will only save the cache if all steps in the job succeed,
      # so we separate the cache restore and save steps in case running dialyzer fails.
      - name: Save lib Dialyzer PLT cache
        id: save-lib-dialyzer-plt-cache
        uses: actions/cache/save@v6
        if: ${{ env.IS_HIGHEST_MATRIX_COMBINATION == 'true' && steps.restore-lib-dialyzer-plt-cache.outputs.cache-hit != 'true' }}
        with:
          key: |
            dialyzer-plt-lib-${{ runner.os }}-${{ matrix.elixir }}-${{ matrix.otp }}-${{ hashFiles('mix.lock') }}
          path: |
            priv/plts

      # Step: Run lib Elixir static code analysis with Dialyzer.
      - name: Run lib Elixir static code analysis with Dialyzer
        if: ${{ env.IS_HIGHEST_MATRIX_COMBINATION == 'true' }}
        run: mix dialyzer --format github

      # Step: Check that all lib test scripts have valid file names.
      - name: Check lib test file names
        if: ${{ env.IS_HIGHEST_MATRIX_COMBINATION == 'true' }}
        run: mix holo.test.check_file_names test/elixir

      # Step: Run lib JavaScript static code analysis with ESLint.
      - name: Run lib JavaScript static code analysis with ESLint
        if: ${{ env.IS_HIGHEST_MATRIX_COMBINATION == 'true' }}
        run: mix eslint

      # Step: Execute lib Elixir tests.
      - name: Run lib Elixir tests
        run: mix test --warnings-as-errors --exclude consistency

      # Step: Execute lib Elixir consistency tests.
      - name: Run lib Elixir consistency tests
        if: ${{ env.IS_HIGHEST_MATRIX_COMBINATION == 'true' }}
        run: mix test --warnings-as-errors --only consistency

      # Step: Execute lib JavaScript tests.
      - name: Run lib JavaScript tests
        run: mix test.js

  feature_tests:
    runs-on: ubuntu-latest
    name: Feature Tests (partition ${{ matrix.partition }}) on ubuntu-latest, Elixir ${{ matrix.elixir }}, OTP ${{ matrix.otp }}, Node.js ${{ matrix.node }}
    defaults:
      run:
        working-directory: test/features
    strategy:
      fail-fast: false
      # Specify the Elixir and OTP versions to use when building
      # and running the workflow steps.
      matrix:
        elixir:
          # Run feature tests on latest pinned stable Elixir version only
          - "1.20.0"
        otp:
          # Run feature tests on latest pinned stable OTP version only
          - "29.0"
        node:
          # Run feature tests on latest LTS version only.
          # Must match .tool-versions for dev/prod/CI parity.
          - "24.13.0"
        # The suite is split across runners rather than run concurrently within one,
        # because a browser-driven suite is what exhausted a single runner before -
        # see the max_cases note in test/features/test/test_helper.exs. Each partition
        # is its own runner running one browser at a time, so per-runner load is
        # unchanged no matter how many partitions there are.
        #
        # Two bounds pin the count. Below, no partition count beats ~2 minutes:
        # operators_test.exs holds 44 of the suite's ~600 tests and a single file
        # cannot be split across partitions, and every partition repeats ~50s of
        # checkout, setup and compile. Above, the account runs at most 20 concurrent
        # jobs, and the other jobs in this workflow take 7 of them.
        #
        # 8 is the policy choice between them: ~4 minutes, 15 concurrent jobs, and
        # enough headroom that no partition waits for a runner. More partitions keep
        # helping - 12 would save a further ~40s - but at 19 jobs a queued partition
        # would cost more than the balance gains.
        #
        # Must stay in sync with --partitions in the test step below. A partition
        # listed here but outside that range fails loudly, but dropping one silently
        # skips its files.
        partition: [1, 2, 3, 4, 5, 6, 7, 8]
    env:
      # Everything that inspects the codebase rather than running it is identical on
      # every partition, so it runs on one. Without this each partition would repeat
      # the analysis, and on a cold cache all of them would build the Dialyzer PLT and
      # race to save the same key.
      IS_FIRST_PARTITION: ${{ matrix.partition == 1 }}
      MIX_TEST_PARTITION: ${{ matrix.partition }}
    steps:
      # Step: Setup Elixir + Erlang/OTP image as the base.
      - name: Set up Elixir
        uses: erlef/setup-beam@v1
        with:
          hexpm-mirrors: |
            https://builds.hex.pm
            https://cdn.jsdelivr.net/hex
          elixir-version: ${{ matrix.elixir }}
          otp-version: ${{ matrix.otp }}

      # Step: Checkout the code.
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      # Step: Setup Node.js.
      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: ${{ matrix.node }}

      ### FEATURE TESTS APP CHECKS ###

      # Step: Define how to cache the feature tests app Elixir dependencies.
      - name: Cache feature tests app Elixir deps
        id: cache-feature-tests-app-elixir-deps
        uses: actions/cache@v6
        env:
          cache-name: cache-feature-tests-app-elixir-deps
        with:
          path: test/features/deps
          key: ${{ env.cache-name }}-${{ runner.os }}-${{ matrix.elixir }}-${{ matrix.otp }}-${{ hashFiles('test/features/mix.lock') }}

      # Step: Define how to cache the feature tests app Elixir build.
      - name: Cache feature tests app Elixir build
        id: cache-feature-tests-app-elixir-build
        uses: actions/cache@v6
        env:
          cache-name: cache-feature-tests-app-elixir-build
        with:
          path: test/features/_build
          key: ${{ env.cache-name }}-${{ runner.os }}-${{ matrix.elixir }}-${{ matrix.otp }}-${{ hashFiles('test/features/mix.lock') }}

      # Step: Download feature tests app Elixir dependencies.
      # If unchanged, uses the cached version.
      - name: Download feature tests app Elixir deps
        uses: nick-fields/retry@v4
        with:
          # TODO: set working directory with action input when https://github.com/nick-fields/retry/issues/89 is implemented
          command: cd test/features && mix deps.get
          shell: bash
          timeout_minutes: 3
          max_attempts: 10

      # Step: Download feature tests app JavaScript dependencies.
      - name: Download feature tests app JavaScript deps
        uses: nick-fields/retry@v4
        with:
          command: cd test/features/assets && npm clean-install
          shell: bash
          timeout_minutes: 3
          max_attempts: 10

      # Step: Compile the feature tests app project treating any warnings as errors.
      - name: Check feature tests app project compiles without warnings
        run: mix compile --all-warnings --warnings-as-errors

      # Step: Check if feature tests app mix.lock has any unused dependencies.
      - name: Check feature tests app unused Elixir deps
        if: ${{ env.IS_FIRST_PARTITION == 'true' }}
        run: mix deps.unlock --check-unused

      # Step: Check if there are any feature tests app Elixir dependencies which have been marked as retired.
      - name: Check feature tests app retired Elixir deps
        if: ${{ env.IS_FIRST_PARTITION == 'true' }}
        run: mix hex.audit

      # Step: Check that the feature tests app Elixir code has already been formatted.
      - name: Check feature tests app Elixir code formatted
        if: ${{ env.IS_FIRST_PARTITION == 'true' }}
        run: mix format --check-formatted

      # Step: Run feature tests app Elixir static code analysis with Credo.
      - name: Run feature tests app Elixir static code analysis with Credo
        if: ${{ env.IS_FIRST_PARTITION == 'true' }}
        run: mix credo --strict

      # Step: Restore feature tests app Dialyzer PLT cache.
      # Cache key is based on Erlang/Elixir version and the mix.lock hash.
      - name: Restore feature tests app Dialyzer PLT cache
        id: restore-feature-tests-app-dialyzer-plt-cache
        uses: actions/cache/restore@v6
        with:
          key: |
            dialyzer-plt-feature-tests-app-${{ runner.os }}-${{ matrix.elixir }}-${{ matrix.otp }}-${{ hashFiles('test/features/mix.lock') }}
          path: |
            test/features/priv/plts
        if: ${{ env.IS_FIRST_PARTITION == 'true' }}

      # Step: Create feature tests app Dialyzer PLTs if no cache was found.
      - name: Create feature tests app Dialyzer PLTs
        if: ${{ env.IS_FIRST_PARTITION == 'true' && steps.restore-feature-tests-app-dialyzer-plt-cache.outputs.cache-hit != 'true' }}
        run: mix dialyzer --plt

      # Step: Save feature tests app Dialyzer PLT cache.
      # By default, the GitHub Cache action will only save the cache if all steps in the job succeed,
      # so we separate the cache restore and save steps in case running dialyzer fails.
      - name: Save feature tests app Dialyzer PLT cache
        id: save-feature-tests-app-dialyzer-plt-cache
        uses: actions/cache/save@v6
        if: ${{ env.IS_FIRST_PARTITION == 'true' && steps.restore-feature-tests-app-dialyzer-plt-cache.outputs.cache-hit != 'true' }}
        with:
          key: |
            dialyzer-plt-feature-tests-app-${{ runner.os }}-${{ matrix.elixir }}-${{ matrix.otp }}-${{ hashFiles('test/features/mix.lock') }}
          path: |
            test/features/priv/plts

      # Step: Run feature tests app Elixir static code analysis with Dialyzer.
      - name: Run feature tests app Elixir static code analysis with Dialyzer
        if: ${{ env.IS_FIRST_PARTITION == 'true' }}
        run: mix dialyzer --format github

      # Step: Check that all feature tests app test scripts have valid file names.
      - name: Check feature tests app test file names
        if: ${{ env.IS_FIRST_PARTITION == 'true' }}
        run: mix holo.test.check_file_names test

      # Step: Execute this partition's share of the feature tests app Elixir tests.
      # Mix sorts the test files and deals them round-robin across the partitions,
      # selecting this runner's share with MIX_TEST_PARTITION, so new test files are
      # picked up without a list to maintain. Must stay in sync with the partition
      # matrix above.
      - name: Run feature tests app Elixir tests
        run: mix test --warnings-as-errors --partitions 8

      # Step: Archive the browser screenshots of tests that failed.
      - name: Archive fail screenshots
        # Execute the step even if the job has already failed, but don't execute it if the job has been canceled.
        if: success() || failure()
        uses: actions/upload-artifact@v7
        with:
          name: fail-screenshots-${{ matrix.elixir }}-${{ matrix.otp }}-${{ matrix.node }}-partition-${{ matrix.partition }}
          path: test/features/tmp/screenshots

  cluster_tests:
    runs-on: ubuntu-latest
    name: Cluster Tests on ubuntu-latest, Elixir ${{ matrix.elixir }}, OTP ${{ matrix.otp }}, Node.js ${{ matrix.node }}
    defaults:
      run:
        working-directory: test/cluster
    strategy:
      fail-fast: false
      # Specify the Elixir and OTP versions to use when building
      # and running the workflow steps.
      matrix:
        elixir:
          # Run cluster tests on latest pinned stable Elixir version only
          - "1.20.0"
        otp:
          # Run cluster tests on latest pinned stable OTP version only
          - "29.0"
        node:
          # Run cluster tests on latest LTS version only.
          # Must match .tool-versions for dev/prod/CI parity.
          - "24.13.0"
    env:
      IS_HIGHEST_MATRIX_COMBINATION: "true"
    steps:
      # Step: Setup Elixir + Erlang/OTP image as the base.
      - name: Set up Elixir
        uses: erlef/setup-beam@v1
        with:
          hexpm-mirrors: |
            https://builds.hex.pm
            https://cdn.jsdelivr.net/hex
          elixir-version: ${{ matrix.elixir }}
          otp-version: ${{ matrix.otp }}

      # Step: Checkout the code.
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      # Step: Setup Node.js.
      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: ${{ matrix.node }}

      ### CLUSTER TESTS APP CHECKS ###

      # Step: Define how to cache the cluster tests app Elixir dependencies.
      - name: Cache cluster tests app Elixir deps
        id: cache-cluster-tests-app-elixir-deps
        uses: actions/cache@v6
        env:
          cache-name: cache-cluster-tests-app-elixir-deps
        with:
          path: test/cluster/deps
          key: ${{ env.cache-name }}-${{ runner.os }}-${{ matrix.elixir }}-${{ matrix.otp }}-${{ hashFiles('test/cluster/mix.lock') }}

      # Step: Define how to cache the cluster tests app Elixir build.
      - name: Cache cluster tests app Elixir build
        id: cache-cluster-tests-app-elixir-build
        uses: actions/cache@v6
        env:
          cache-name: cache-cluster-tests-app-elixir-build
        with:
          path: test/cluster/_build
          key: ${{ env.cache-name }}-${{ runner.os }}-${{ matrix.elixir }}-${{ matrix.otp }}-${{ hashFiles('test/cluster/mix.lock') }}

      # Step: Download cluster tests app Elixir dependencies.
      # If unchanged, uses the cached version.
      - name: Download cluster tests app Elixir deps
        uses: nick-fields/retry@v4
        with:
          # TODO: set working directory with action input when https://github.com/nick-fields/retry/issues/89 is implemented
          command: cd test/cluster && mix deps.get
          shell: bash
          timeout_minutes: 3
          max_attempts: 10

      # Step: Compile the cluster tests app project treating any warnings as errors.
      - name: Check cluster tests app project compiles without warnings
        run: mix compile --all-warnings --warnings-as-errors

      # Step: Check if cluster tests app mix.lock has any unused dependencies.
      - name: Check cluster tests app unused Elixir deps
        if: ${{ env.IS_HIGHEST_MATRIX_COMBINATION == 'true' }}
        run: mix deps.unlock --check-unused

      # Step: Check if there are any cluster tests app Elixir dependencies which have been marked as retired.
      - name: Check cluster tests app retired Elixir deps
        if: ${{ env.IS_HIGHEST_MATRIX_COMBINATION == 'true' }}
        run: mix hex.audit

      # Step: Check that the cluster tests app Elixir code has already been formatted.
      - name: Check cluster tests app Elixir code formatted
        if: ${{ env.IS_HIGHEST_MATRIX_COMBINATION == 'true' }}
        run: mix format --check-formatted

      # Step: Run cluster tests app Elixir static code analysis with Credo.
      - name: Run cluster tests app Elixir static code analysis with Credo
        if: ${{ env.IS_HIGHEST_MATRIX_COMBINATION == 'true' }}
        run: mix credo --strict

      # Step: Restore cluster tests app Dialyzer PLT cache.
      # Cache key is based on Erlang/Elixir version and the mix.lock hash.
      - name: Restore cluster tests app Dialyzer PLT cache
        id: restore-cluster-tests-app-dialyzer-plt-cache
        uses: actions/cache/restore@v6
        with:
          key: |
            dialyzer-plt-cluster-tests-app-${{ runner.os }}-${{ matrix.elixir }}-${{ matrix.otp }}-${{ hashFiles('test/cluster/mix.lock') }}
          path: |
            test/cluster/priv/plts
        if: ${{ env.IS_HIGHEST_MATRIX_COMBINATION == 'true' }}

      # Step: Create cluster tests app Dialyzer PLTs if no cache was found.
      - name: Create cluster tests app Dialyzer PLTs
        if: ${{ env.IS_HIGHEST_MATRIX_COMBINATION == 'true' && steps.restore-cluster-tests-app-dialyzer-plt-cache.outputs.cache-hit != 'true' }}
        run: mix dialyzer --plt

      # Step: Save cluster tests app Dialyzer PLT cache.
      # By default, the GitHub Cache action will only save the cache if all steps in the job succeed,
      # so we separate the cache restore and save steps in case running dialyzer fails.
      - name: Save cluster tests app Dialyzer PLT cache
        id: save-cluster-tests-app-dialyzer-plt-cache
        uses: actions/cache/save@v6
        if: ${{ env.IS_HIGHEST_MATRIX_COMBINATION == 'true' && steps.restore-cluster-tests-app-dialyzer-plt-cache.outputs.cache-hit != 'true' }}
        with:
          key: |
            dialyzer-plt-cluster-tests-app-${{ runner.os }}-${{ matrix.elixir }}-${{ matrix.otp }}-${{ hashFiles('test/cluster/mix.lock') }}
          path: |
            test/cluster/priv/plts

      # Step: Run cluster tests app Elixir static code analysis with Dialyzer.
      - name: Run cluster tests app Elixir static code analysis with Dialyzer
        if: ${{ env.IS_HIGHEST_MATRIX_COMBINATION == 'true' }}
        run: mix dialyzer --format github

      # Step: Check that all cluster tests app test scripts have valid file names.
      - name: Check cluster tests app test file names
        if: ${{ env.IS_HIGHEST_MATRIX_COMBINATION == 'true' }}
        run: mix holo.test.check_file_names test

      # Step: Execute cluster tests app Elixir tests.
      - name: Run cluster tests app Elixir tests
        run: mix test --warnings-as-errors

      # Step: Archive the browser screenshots of tests that failed.
      - name: Archive fail screenshots
        # Execute the step even if the job has already failed, but don't execute it if the job has been canceled.
        if: success() || failure()
        uses: actions/upload-artifact@v7
        with:
          name: cluster-fail-screenshots-${{ matrix.elixir }}-${{ matrix.otp }}-${{ matrix.node }}
          path: test/cluster/tmp/screenshots
  umbrella_tests:
    runs-on: ubuntu-latest
    name: Umbrella Tests on ubuntu-latest, Elixir ${{ matrix.elixir }}, OTP ${{ matrix.otp }}, Node.js ${{ matrix.node }}
    defaults:
      run:
        working-directory: test/umbrella
    strategy:
      fail-fast: false
      # Specify the Elixir and OTP versions to use when building
      # and running the workflow steps.
      matrix:
        elixir:
          # Run umbrella tests on latest pinned stable Elixir version only
          - "1.20.0"
        otp:
          # Run umbrella tests on latest pinned stable OTP version only
          - "29.0"
        node:
          # Run umbrella tests on latest LTS version only.
          # Must match .tool-versions for dev/prod/CI parity.
          - "24.13.0"
    env:
      IS_HIGHEST_MATRIX_COMBINATION: "true"
    steps:
      # Step: Setup Elixir + Erlang/OTP image as the base.
      - name: Set up Elixir
        uses: erlef/setup-beam@v1
        with:
          hexpm-mirrors: |
            https://builds.hex.pm
            https://cdn.jsdelivr.net/hex
          elixir-version: ${{ matrix.elixir }}
          otp-version: ${{ matrix.otp }}

      # Step: Checkout the code.
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      # Step: Setup Node.js.
      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: ${{ matrix.node }}

      ### UMBRELLA TESTS APP CHECKS ###

      # Step: Define how to cache the umbrella tests app Elixir dependencies.
      - name: Cache umbrella tests app Elixir deps
        id: cache-umbrella-tests-app-elixir-deps
        uses: actions/cache@v6
        env:
          cache-name: cache-umbrella-tests-app-elixir-deps
        with:
          path: test/umbrella/deps
          key: ${{ env.cache-name }}-${{ runner.os }}-${{ matrix.elixir }}-${{ matrix.otp }}-${{ hashFiles('test/umbrella/mix.lock') }}

      # Step: Define how to cache the umbrella tests app Elixir build.
      - name: Cache umbrella tests app Elixir build
        id: cache-umbrella-tests-app-elixir-build
        uses: actions/cache@v6
        env:
          cache-name: cache-umbrella-tests-app-elixir-build
        with:
          path: test/umbrella/_build
          key: ${{ env.cache-name }}-${{ runner.os }}-${{ matrix.elixir }}-${{ matrix.otp }}-${{ hashFiles('test/umbrella/mix.lock') }}

      # Step: Download umbrella tests app Elixir dependencies.
      # If unchanged, uses the cached version.
      - name: Download umbrella tests app Elixir deps
        uses: nick-fields/retry@v4
        with:
          # TODO: set working directory with action input when https://github.com/nick-fields/retry/issues/89 is implemented
          command: cd test/umbrella && mix deps.get
          shell: bash
          timeout_minutes: 3
          max_attempts: 10

      # Step: Download umbrella tests app JavaScript dependencies.
      - name: Download umbrella tests app JavaScript deps
        uses: nick-fields/retry@v4
        with:
          command: cd test/umbrella/assets && npm clean-install
          shell: bash
          timeout_minutes: 3
          max_attempts: 10

      # Step: Compile the umbrella tests app project treating any warnings as errors.
      - name: Check umbrella tests app project compiles without warnings
        run: mix compile --all-warnings --warnings-as-errors

      # Step: Check if umbrella tests app mix.lock has any unused dependencies.
      - name: Check umbrella tests app unused Elixir deps
        if: ${{ env.IS_HIGHEST_MATRIX_COMBINATION == 'true' }}
        run: mix deps.unlock --check-unused

      # Step: Check if there are any umbrella tests app Elixir dependencies which have been marked as retired.
      - name: Check umbrella tests app retired Elixir deps
        if: ${{ env.IS_HIGHEST_MATRIX_COMBINATION == 'true' }}
        run: mix hex.audit

      # Step: Check that the umbrella tests app Elixir code has already been formatted.
      - name: Check umbrella tests app Elixir code formatted
        if: ${{ env.IS_HIGHEST_MATRIX_COMBINATION == 'true' }}
        run: mix format --check-formatted

      # Step: Run umbrella tests app Elixir static code analysis with Credo.
      - name: Run umbrella tests app Elixir static code analysis with Credo
        if: ${{ env.IS_HIGHEST_MATRIX_COMBINATION == 'true' }}
        run: mix credo --strict

      # Step: Restore umbrella tests app Dialyzer PLT cache.
      # Cache key is based on Erlang/Elixir version and the mix.lock hash.
      - name: Restore umbrella tests app Dialyzer PLT cache
        id: restore-umbrella-tests-app-dialyzer-plt-cache
        uses: actions/cache/restore@v6
        with:
          key: |
            dialyzer-plt-umbrella-tests-app-${{ runner.os }}-${{ matrix.elixir }}-${{ matrix.otp }}-${{ hashFiles('test/umbrella/mix.lock') }}
          path: |
            test/umbrella/priv/plts
        if: ${{ env.IS_HIGHEST_MATRIX_COMBINATION == 'true' }}

      # Step: Create umbrella tests app Dialyzer PLTs if no cache was found.
      - name: Create umbrella tests app Dialyzer PLTs
        if: ${{ env.IS_HIGHEST_MATRIX_COMBINATION == 'true' && steps.restore-umbrella-tests-app-dialyzer-plt-cache.outputs.cache-hit != 'true' }}
        run: mix dialyzer --plt

      # Step: Save umbrella tests app Dialyzer PLT cache.
      # By default, the GitHub Cache action will only save the cache if all steps in the job succeed,
      # so we separate the cache restore and save steps in case running dialyzer fails.
      - name: Save umbrella tests app Dialyzer PLT cache
        id: save-umbrella-tests-app-dialyzer-plt-cache
        uses: actions/cache/save@v6
        if: ${{ env.IS_HIGHEST_MATRIX_COMBINATION == 'true' && steps.restore-umbrella-tests-app-dialyzer-plt-cache.outputs.cache-hit != 'true' }}
        with:
          key: |
            dialyzer-plt-umbrella-tests-app-${{ runner.os }}-${{ matrix.elixir }}-${{ matrix.otp }}-${{ hashFiles('test/umbrella/mix.lock') }}
          path: |
            test/umbrella/priv/plts

      # Step: Run umbrella tests app Elixir static code analysis with Dialyzer.
      - name: Run umbrella tests app Elixir static code analysis with Dialyzer
        if: ${{ env.IS_HIGHEST_MATRIX_COMBINATION == 'true' }}
        run: mix dialyzer --format github

      # Step: Check that all umbrella tests app test scripts have valid file names.
      - name: Check umbrella tests app test file names
        if: ${{ env.IS_HIGHEST_MATRIX_COMBINATION == 'true' }}
        run: mix holo.test.check_file_names apps/app_1/test

      # Step: Execute umbrella tests app Elixir tests.
      - name: Run umbrella tests app Elixir tests
        run: mix test --warnings-as-errors

      # Step: Archive the browser screenshots of tests that failed.
      - name: Archive fail screenshots
        # Execute the step even if the job has already failed, but don't execute it if the job has been canceled.
        if: success() || failure()
        uses: actions/upload-artifact@v7
        with:
          name: umbrella-fail-screenshots-${{ matrix.elixir }}-${{ matrix.otp }}-${{ matrix.node }}
          path: test/umbrella/tmp/screenshots
